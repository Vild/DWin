Info.AddCtor("init.ds", function() {
	BindManager.Map("Ctrl + Enter", function(var pressed) {
		if (pressed)
			System.Exec("xterm");
	});

	BindManager.Map("Ctrl + Backspace", function(var pressed) {
		if (pressed)
			System.Exec("xeyes");
	});
});